# Success Metrics

## Developer adoption

```text
GitHub stars
CLI installs
Docker pulls
example runs
MCP gateway starts
```

## Product usage

```text
agents registered
tools discovered
MCP servers scanned
tool calls proxied
policies applied
approvals processed
ABOMs generated
```

## Business value

```text
risky tool calls blocked
external writes approved
unknown tools discovered
MCP schema changes detected
agent inventory coverage
audit export usage
```

## Enterprise readiness

```text
SSO requests
self-hosted installs
security review requests
policy pack usage
Kubernetes installs
SIEM integration requests
```

## MVP health dashboard

| Metric | Why it matters |
|---|---|
| Tool calls proxied | Shows the gateway is in the path of real agent work. |
| Policies applied | Shows governance is configured, not just observed. |
| Approval decisions | Shows risky work is being controlled. |
| ABOMs generated | Shows inventory can become compliance evidence. |
| MCP schema diffs | Shows the product catches tool drift. |
